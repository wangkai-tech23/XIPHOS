You can generate the edge files required for training through our "!generate ROAD edges 400_5.py", which includes three parts: graph property generation, data slice and data movement.

You can generate the node vector files required for training through our "!generate ROAD nodes 400_5.py", which includes three parts: graph property generation, data slice and data movement.

You can get the node vector files after batch data merging through our "!generate ROAD merge 400node vectors.py".  Its output is two csv file named "train_nodes" and "test_nodes".